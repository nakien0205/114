# Data.yaml is added to .gitignore to avoid multiple users changing the path to their local machine. 
# Please change the path to your local machine before running the code.
path: datasets/DFire
train: train/images
val: val/images
test: test/images

nc: 2
names:
  0: fire
  1: smoke
